apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: test-lee
  labels:
    app: nginx
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 80
    nodePort: 30080
    protocol: TCP
  selector:
    app: nginx
